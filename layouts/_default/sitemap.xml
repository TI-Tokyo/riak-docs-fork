<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {{- $all_pages := where (where (where (where .Site.RegularPages ".Params.project" "!=" nil) ".Params.project_version" "!=" nil) ".Params.exclude_from_search" "!=" true) ".Params.exclude_from_sitemap" "!=" true -}}
    {{- range $index, $page := $all_pages -}}
    <url><loc>{{- $page.Permalink -}}</loc></url>
    {{- end -}}
</urlset>